{
  "name": "@watchmen/containr",
  "version": "1.1.3",
  "description": "run cli in containers",
  "type": "module",
  "exports": {
    ".": "./src/index.js",
    "./oci": "./src/oci.js",
    "./util": "./src/util.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/the-watchmen/node-containr.git"
  },
  "scripts": {
    "pretest": "xo",
    "test": "npm run ava",
    "posttest": "npm run clear",
    "ava": "DEBUG=dbg:* NODE_ENV=test npx ava --serial",
    "pretest-container": "docker build -t $(npm run tag --silent) --progress=plain --target=test .",
    "test-container": "docker run -v /var/run/docker.sock:/var/run/docker.sock -v /tmp:/tmp -u $(npm run user --silent)  $(npm run tag --silent)",
    "posttest-container": "npm run clear",
    "tagx": "echo ${npm_package_name}:${npm_package_version}-$(git rev-parse --short HEAD^) | cut -c2-",
    "tag": "echo ${npm_package_name}:${npm_package_version} | cut -c2-",
    "user": "echo $(id -u)",
    "clear": "rm -rf /tmp/containr"
  },
  "author": "tony kerz",
  "license": "ISC",
  "dependencies": {
    "@watchmen/debug": "^2.0.8",
    "@watchmen/helpr": "^3.0.0",
    "config": "^3.3.12",
    "dayjs": "^1.11.13",
    "execa": "^9.5.2",
    "fs-extra": "^11.2.0",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "ava": "^6.2.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.2.1",
    "prettier": "^3.4.2",
    "xo": "^0.60.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
